spring:

  datasource:

    url: jdbc:mariadb://localhost:3306/identity_db

    username: root

    password: rootpassword

    driver-class-name: org.mariadb.jdbc.Driver

  jpa:

    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

jwt:

  secret: mysecretkeymysecretkey123456

  expire: 86400000